bintray {
    user = System.getenv('BINTRAY_USER')
    key = System.getenv('BINTRAY_KEY')
    publications = ['publishjob']
    pkg {
        repo = 'opensource'
        name = 'redisson-starter'
        licenses = ['Apache-2.0']
        vcsUrl = 'https://github.com/SVT/redisson-starter'
        userOrg = 'svt'

        version {
            name = scmVersion.version
        }
    }
}
